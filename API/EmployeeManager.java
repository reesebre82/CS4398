package API;

import java.util.ArrayList;

public class EmployeeManager {
    ArrayList<Employee> employees;

    // public EmployeeManager() Empty constructor that instantiates employees as
    // empty arraylist or get data from database//brendan will implement this

    // Employee getEmployee(String firstName, String lastName) function that returns
    // a employee with a given first and last name

    // Employee getEmployee(int customerID) function that returns a customer with a
    // given Employee ID

    // Void updateEmployee(int employeeID, Employee updatedEmployee) where it will
    // update the employee with the same employeeID in employees(after completed,
    // brendan will add a section to update database)

    // deconstructor to push data to database //Brendan will implement this;
}